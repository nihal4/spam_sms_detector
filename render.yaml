services:
  - type: web
    name: sms-spam-detector
    runtime: python
    buildCommand: pip install fastapi "uvicorn[standard]" scikit-learn jinja2 python-multipart
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
